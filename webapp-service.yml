apiVersion: v1
kind: Service
metadata:
  name: pitstop-webapp
  namespace: pitstop
spec:
  selector:
    app: pitstop-webapp
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8080
  type: ClusterIP